import React from 'react';

export default class Enonic extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function
	render() {
		return (
			<div>
		    	Enonic loves React!
		    </div>
		);
	}
}
